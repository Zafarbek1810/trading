/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: false,
  output: 'standalone',
  trailingSlash:true,
  // swcMinify: true,
  // compiler: {
  //   styledComponents: true
  // }
}

module.exports = nextConfig
